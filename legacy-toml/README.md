# Legacy TOML snapshot

Point-in-time copy of the dogfood plan while it was still TOML on disk
(pre-M92 JSON migration). Preserves the full ceremony scaffolding
(`[behavior]`, `success_criteria = []`, etc.) for reference and rollback.

**Canonical plan paths** (`../milestones/`, `../plan.toml`, …) remain active
until M92 migration converts them to JSON. Do not edit files here via `mp` —
this tree is read-only archive.

Captured: 2026-07-02.
