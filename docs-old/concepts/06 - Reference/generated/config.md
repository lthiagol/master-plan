# `mp config`

**Usage:**

```text
Usage: config <COMMAND>
```

**Subcommands:**

| Name | Description |
|------|-------------|
| `show` |  |
| `get` |  |
| `set` |  |
| `validate` | Validate project config (current file, or a candidate via `--file`). Emits JSON `{ ok, errors[{field,message}], warnings[{field,message}] }` |

