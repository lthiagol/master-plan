# `mp backlog`

**Usage:**

```text
Usage: backlog <COMMAND>
```

**Subcommands:**

| Name | Description |
|------|-------------|
| `add` |  |
| `list` | M112 WP1: read-only list of backlog items with AND-combined filters. Output is `{ items: [...] }` regardless of result count — empty backlog returns `{ items: [] }`, not `null` |
| `show` |  |
| `resolve` |  |
| `promote` |  |

