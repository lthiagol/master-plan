# `mp search`

**Usage:**

```text
Usage: search [OPTIONS] <QUERY>
```

**Options:**

| Flag | Description |
|------|-------------|
|  |  |
| `--type` |  |
| `--limit` |  |
| `--include` | Embed the full matched fragment under `hit.object`. Default (`snippet`) returns the snippet-only response to keep payload small |
| `--group-by` | Group hits by their parent milestone in the response |

