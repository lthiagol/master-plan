# tests/raul/

Per-binary test directory for the `raul` binary.

- **Shared scenario fixtures** live under `tests/fixtures/` (used by both `mp` and `raul`).
- `raul` Rust integration tests live in `crates/raul/tests/` (created in M40).
- `raul`-specific golden scenarios will land here.
