# tests/mp/

Per-binary test directory for the `mp` binary.

- **Shared scenario fixtures** live under `tests/fixtures/` (used by both `mp` and `raul`).
- **Golden scenarios** currently live under `tests/scenarios/` (mp-only for now).
- `mp` Rust integration tests live in `crates/mp/tests/`.
